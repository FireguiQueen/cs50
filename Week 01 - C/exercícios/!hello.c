#include <stdio.h> // include = incluir. Ou seja, incluir uma biblioteca.


int main(void) // define a função principal (void é usado quando a função não irá receber args)
{   // define o escopo da função
    printf("Olá mundo!\n") // printf printa no console. \n (newline) serve para o próximo print no console seja na proxima linha
    printf("Estou em outra linha.") // printf printa no console. \n (newline) serve para o próximo print no console seja na proxima linha
}   // fecha o escopo da funçao